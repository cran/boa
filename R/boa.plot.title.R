"boa.plot.title" <-
function(text = boa.par("title"))
#-------------------------------------------------------------------------------
#-------------------------------------------------------------------------------
{
   mtext(text, line = 1, outer = TRUE, cex = 2)
   invisible()
}
