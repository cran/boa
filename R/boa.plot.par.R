"boa.plot.par" <-
function(mfdim = c(1, 1))
#-------------------------------------------------------------------------------
#-------------------------------------------------------------------------------
{
   par(mfrow = mfdim, oma = c(0, 0, 3 + max(mfdim), 0))
   invisible()
}
